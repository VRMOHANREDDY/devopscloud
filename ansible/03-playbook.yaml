- name: Install nginx
  hosts: frontend
  become: yes # become means rootserver like -b in commandline
  tasks: 
  - name: install nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: start nginx
    service:
      name: nginx
      state: started
      enabled: yes