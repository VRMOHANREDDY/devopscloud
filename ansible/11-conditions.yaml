- name: conditions
  hosts: local
  connection: local
  vars:
    session_count: 7
  tasks:
  - name: conditions check
    debug:
      msg: "Anisble takes more than 5 sessions"
    when: session_count > 5